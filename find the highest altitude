class Solution {
    public int largestAltitude(int[] gain) {
        int heighest=0;
        int sum=0;
        for(int i=0;i<gain.length;i++)
        {
            sum+=gain[i];
            heighest=Math.max(sum,heighest);
        }
        return heighest;
        
    }
}
